import 'package:flutter/material.dart';

class HourlyInfo extends StatelessWidget {
  const HourlyInfo({super.key});
  @override
  Widget build(BuildContext context) {
    return Text('prova');
  }
}
